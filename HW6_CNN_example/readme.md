# About HW3 Image Processing

This is a Python example of image classification.

To run it you have to install pytorch and matplotlib.

After all requirements are satisfies, run:
```shell
python mnist_example.py
```